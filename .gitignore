# Ingested data. Never commit the corpus.
data/
*.json.gz

# Design prototype for the chat UI, and the files unpacked from it. Held back
# until the real demo is built against the live agent, because the prototype
# runs on invented rules and pull request numbers.
demo/index.html
demo/_extracted/

# Secrets
.env
.env.local
*.pem
*.crt

# Working notes: schedule, spend, and what was deferred. Kept out of the
# public repo. The technical decisions in here belong in the README instead.
docs/progress.md
docs/video-script.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# Logs
*.log
logs/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
